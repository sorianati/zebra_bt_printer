/// Defaults de geometría de etiqueta (impresión por job).
abstract final class LabelDefaults {
  LabelDefaults._();

  static const defaultLabelWidthDots = 600;
  static const defaultLabelHeightDots = 240;
  static const maxLabelLengthMultiplier = 2;
  static const defaultLabelTopOffset = 0;
}
